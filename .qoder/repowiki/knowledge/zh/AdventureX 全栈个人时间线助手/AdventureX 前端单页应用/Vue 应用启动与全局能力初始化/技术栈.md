Vue 3 + TypeScript + Vite；样式使用 Tailwind CSS v4（`@import "tailwindcss"`）；PWA 通过 `beforeinstallprompt` / `appinstalled` 事件与 `display-mode: standalone` 检测；实时同步基于原生 WebSocket 并以每 10 秒轮询 `triggerRefresh` 自定义事件作为降级方案。