FastAPI 0.115+ 作为 Web 框架，SQLAlchemy 2.0 + aiosqlite 异步 ORM，Pydantic v2 做数据验证，httpx 异步 HTTP 客户端（避免 OpenAI SDK 触发 Cloudflare WAF），websockets 16.x 用于 ASR 实时流，uvicorn 作为 ASGI 服务器。LLM 通过自定义 Cloudflare Worker Relay 访问 DeepSeek v4-flash，ASR 使用 Qwen3-ASR 实时流式转写。