- 副作用模块通过顶层 import 在 `main.ts` 中一次性加载，利用模块加载时机完成事件监听器注册，而非显式调用初始化函数。
- 全局状态通过 Vue 的 `ref` 暴露为响应式变量（如 `syncConnected`、`dismissedAt`），供组件直接订阅更新。
- 浏览器 API 访问统一包裹 `typeof window !== 'undefined'` 守卫，保证 SSR 安全。
- 本地存储读写统一使用 try/catch 包裹，防止第三方禁用 localStorage 导致崩溃。