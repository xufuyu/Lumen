启动方式：`python -m uvicorn main:app --host 0.0.0.0 --port 8000 --reload`（见 `main.py` 的 `__main__`）。首次运行会自动创建 SQLite 数据库文件 `data/adventurex.db` 并执行列级迁移（添加 user_id、started_at 字段）。需要配置 `.env` 中的 `DATABASE_URL`、`RELAY_BASE_URL`、`RELAY_API_KEY`、`ASR_RELAY_WS_URL` 等环境变量，可参考 `.env.example`。