本地开发：cd backend && pip install -r requirements.txt && python main.py 启动 API（:8000），cd frontend && npm install && npm run dev 启动 Vite 开发服务器（:5173，自动代理到后端）。一键部署：DEPLOY_HOST=x.x.x.x DEPLOY_USER=ubuntu RELAY_CA_PEM_FILE=./ca.pem bash deploy.sh，脚本会构建前端、打包后端、替换中继域名、注入证书并通过 rsync+ssh 在远端安装 systemd 服务。