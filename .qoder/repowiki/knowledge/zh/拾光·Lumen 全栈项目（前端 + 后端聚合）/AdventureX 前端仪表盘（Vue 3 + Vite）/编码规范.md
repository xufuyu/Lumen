- 所有页面以 `.vue` 单文件组件形式放在 `src/views/`，通用组件放在 `src/components/`，按功能命名（如 DashboardView.vue、TaskCard.vue）。
- 全局能力（路由、国际化、WebSocket 同步、用户状态）在 `main.ts` 中集中注册，组件内通过组合式函数或注入方式使用。
- API 请求统一通过 `src/api/client.ts` 封装，避免在组件中直接发起 fetch/axios 调用。
- 国际化文案集中在 `src/i18n/locales/{en,zh-CN}.ts`，组件中使用 `$t()` 访问。